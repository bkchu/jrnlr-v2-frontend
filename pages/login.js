import { useState } from 'react';
import { Box, Button, Form, Input } from '../components';
import { LogIn } from '../components/Mutation/LogIn';

export default function Login() {
  return <LogIn />;
}
